mixin heading
  .col-md-4
    h2 Heading
    p Donec id elit non mi porta gravida at eget metus. Fusce dapibus, tellus ac cursus commodo, tortor mauris condimentum nibh, ut fermentum massa justo sit amet risus. Etiam porta sem malesuada magna mollis euismod. Donec sed odio dui.
    p: a.btn.btn-secondary(href="#" role="button") View details &raquo;
    
mixin nav-item(href, label)
  if href === active
    li.nav-item.active
      a.nav-link(href=href)=label
  else
    li.nav-item
      a.nav-link(href=href)=label
    
mixin form-group(name, label, placeholder, value)
  .form-group
    label(for=name)=label
    input.form-control(id=name type="text" name=name placeholder=placeholder value=value)
      
mixin form-group-password(label, name)
  .form-group
    label(for="password")=label
    input.form-control(id="password" type="password" name=name placeholder="Password")

mixin book(data)
  img.book(src=data.imageUrl)
